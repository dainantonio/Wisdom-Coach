[build]
  # This tells Netlify your HTML is in the main folder
  publish = "."
  # This tells Netlify where your backend code is
  functions = "netlify/functions"

[functions]
  # This ensures Netlify installs your dependencies before running
  node_bundle = true
